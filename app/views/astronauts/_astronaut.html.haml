%li.portfolio-item.isotope-item{class: astronaut.class_for_search}
  .item-inner
    %img{:alt => "", :src => path_to_image(astronaut.avatar_url)}/
    .overlay
      %a.preview.btn.btn-outlined.btn-primary{:href => astronaut_path(astronaut)}
        = fa_icon "eye"

